# REF_GENIUS_VIRUS_GAME.md

# 1. SERVICE OVERVIEW

* **서비스명**: 지니어스 바이러스 게임 (The Genius: Virus Game Manager)
* **서비스 유형**: 단일 기기(Single-Station / Host-Display) 기반 학급 오프라인 활동 보조 웹 애플리케이션 (Classroom Board Game / Social Deduction Game Manager)
* **목적**: 
  * 초·중등 과학 교육과정(감염병, 세균과 바이러스, 면역 및 백신)의 학습 내용을 몰입형 심리 추리 게임(Social Deduction Game)과 결합하여 학급 놀이 활동으로 운영
  * 교사가 복잡한 접촉 판정(비감염자 간 안전 접촉 시 승점 획득, 감염자와 접촉 시 즉시 감염, 중복 터치 차단, 치료제 사용 복구)을 실시간으로 자동 연산 및 통제하고, 학생들의 개별 점수와 진영 승패를 집계하여 엑셀(XLSX)로 내보낼 수 있도록 지원
* **교육과정 연계**: 초등/중등 과학 '생물과 환경', '감염병과 우리 생활', '세균과 바이러스의 전파 경로(비말, 접촉, 공기/물 등)', '면역과 백신/치료제'
* **대상 사용자**: 
  * **진행자(교사)**: 게임 설정, 최초 감염자 비밀 지목, 라운드 시간 통제, 비밀 교사 대시보드 열람, 데이터 엑셀 추출
  * **참여자(학생)**: 1대1 접촉 시 교탁(교사 PC/태블릿 키오스크) 앞으로 나와 본인 이름을 직접 터치하여 접촉 판정 수행, 백신(치료제) 사용 신청
* **예상 사용 인원**: 학급 단위 15명 ~ 35명 (단일 학급 권장, 다인수 학급 확장 가능)
* **실제 사용 환경**: 초·중·고등학교 일반 교실, 과학실 (전자칠판/대형 TV 화면 프로젝터 + 교사용 PC/태블릿 터치 모니터)
* **사용 기기**: 
  * 메인 디스플레이/터치 키오스크: 교사용 PC, 노트북, 터치스크린 모니터, 전자칠판(Interactive Whiteboard) 또는 태블릿(iPad/Galaxy Tab)
* **서비스의 핵심 기능**:
  1. **사전 학습 인트로 스토리 & 인터랙티브 퀴즈 모달 (Intro Story Modal)**:
     * 10단계 챕터별 감염병 퀴즈(타이핑 애니메이션 효과, 이미지 시각 자료, 초성 힌트 퀴즈)
     * 긴급 재난 문자(Siren/사이렌 오디오 및 긴장감 폰트)를 통한 게임 세계관 몰입 유도
     * 언제든 즉시 건너뛸 수 있는 `건너뛰기(FastForward)` 컨트롤 제공
  2. **단계별 게임 셋업 (Step-by-Step Setup)**:
     * 라운드 제한시간 설정 (추천 시간 안내 및 강제 종료 가능 팁 제공, 3라운드 고정)
     * 한글/엑셀 명부 복사-붙여넣기 지원 일괄 학생 명단 파싱 (`\n` 줄바꿈 분리)
     * 교사 비밀 지정: 최초 감염자(Original Zombie/Virus Carrier) 토글 선택
  3. **실시간 접촉 판정 엔진 (Contact & Infection Engine)**:
     * 2인 동시 선택 후 `터치(Zap)` 버튼 클릭 시 즉시 감염 여부 및 승점 자동 계산
     * **중복 접촉 방지**: 이미 접촉한 조합(`[id1, id2].sort().join('-')`) 영구 기억 및 경고 모달 출력
     * **1인 선택 치료제(Heart)**: 해당 라운드 신규 감염자만 회복(최초 감염자는 치료 불가)
  4. **무활동 패널티 자동화 (Inactivity Infection Rule)**:
     * 라운드 종료 시 한 번도 터치하지 않은 학생(`!touchedThisRound`)을 자동으로 감염자로 전환
  5. **교사 전용 비밀 대시보드 (Hidden Teacher Dashboard)**:
     * 프로젝터 화면에서 학생들에게는 감염자/비감염자 정체가 물음표(`?`)로 가려져 있으나, 교사 모달에서는 실시간 감염 현황, 승점, 전체 히스토리 로그 열람 가능
  6. **데이터 엑셀 다운로드 (XLSX Export)**:
     * 라운드별 터치자1, 상태, 터치자2, 상태, 획득 승점, 누적 승점, 최초감염자여부, 백신사용여부, 타임스탬프 상세 로그 엑셀 내보내기
* **서비스의 핵심 게임/활동 규칙**:
  * **진영**: 비감염자(인간) 진영 vs 감염자(좀비/바이러스) 진영
  * **목표**: 비감염자는 감염을 피하며 다른 비감염자와 터치하여 승점 누적, 감염자는 모든 인간을 감염시킴
  * **판정 규칙**:
    * 비감염자 + 비감염자 터치 ➔ 두 명 모두 안전, 각각 승점 +1점 획득
    * 비감염자 + 감염자 터치 ➔ 비감염자가 감염자로 즉시 전환, 승점 획득 없음 (+0점)
    * 감염자 + 감염자 터치 ➔ 상태 유지, 승점 획득 없음 (+0점)
    * 중복 터치 불가: 게임 전체 동안 동일한 2인은 1번만 접촉 가능
  * **치료제 규칙**:
    * 1인당 기본 1회 사용 가능 (해당 라운드에 감염된 경우에만 비감염자로 복귀, 누적 승점 유지)
    * 최초 감염자는 치료제를 사용해도 치료되지 않음
  * **무활동 규칙**:
    * 라운드 제한시간 내에 누구와도 접촉하지 않은 학생은 라운드 종료 시 강제 감염
  * **최종 승리 조건**:
    * 3라운드 종료 시점에 비감염자가 단 1명이라도 생존해 있으면 **비감염자 진영 승리** (생존자 중 최고 득점자가 MVP)
    * 전원이 감염자가 되면 **감염자 진영 승리**

---

# 2. FINAL USER FLOW

최종 사용자 흐름은 교사(진행자)의 주도하에 단일 키오스크 화면에서 순차적으로 진행된다.

```
[Main (StartView)] 
  │ 
  ├──> [IntroStoryModal: INTRO(텍스트 스크롤) ➔ QUIZ(1~10단계) ➔ OUTRO(긴급 재난문자 & 사이렌)]
  │     (우측 상단 '건너뛰기' 클릭 시 바로 Setup으로 점프 가능)
  │
  ├──> [사용 설명서 대형 가이드 슬라이드 모달 (1장~5장)]
  │
  └──> [소개 영상 유튜브 외부 링크]
         │
         ▼
[SetupConfigView (라운드 시간 설정)]
         │ (다음 단계)
         ▼
[SetupStudentsView (학생 명단 일괄 입력 & 명단 관리)]
         │ (다음 단계: 학생 2명 이상 필수)
         ▼
[SetupZombiesView (최초 감염자 비밀 지목 - 감염자 1명 이상 필수)]
         │ (게임 시작)
         ▼
[GameView (1~3 라운드 메인 터치 스테이션)]
  │  ├── 학생 2명 선택 ➔ [터치] 버튼 ➔ 접촉 판정 & 승점/감염 처리 & 중복 접촉 방지
  │  ├── 학생 1명 선택 ➔ [치료제] 버튼 ➔ 확인 모달 ➔ 치료 처리
  │  ├── 타이머 제어 (일시정지/재개, +10초 연장, 라운드 강제 종료)
  │  ├── [교사 페이지] 모달 (실시간 비밀 감염 현황 & 로그 & 엑셀 추출)
  │  └── [게임 강제 종료] 모달 (초기화 후 메인 이동)
  │
  ├──> [라운드 종료 오버레이 (무활동자 감염 처리 & 라운드 요약)]
  │     │ (다음 라운드 시작 ➔ 2R / 3R 반복)
  │     ▼
[ResultsView (최종 결과 화면)]
  │  ├── 감염자 수 vs 비감염자 수 카운팅 애니메이션 (0부터 증가)
  │  ├── 승리 진영 선포 (비감염자 승리 / 감염자 승리)
  │  ├── 비감염자 생존자 개별 승점 랭킹 리스트 (점수 내림차순 정렬)
  │  ├── 결과 엑셀 다운로드 (XLSX 파일)
  │  └── 처음으로 돌아가기 (새 게임 리셋)
```

### 단계별 상세 흐름표:

| 단계 (Step) | 주체 (Actor) | 사용자 행동 (User Action) | 시스템 행동 (System Response) | 다음 단계 전환 조건 |
| :--- | :--- | :--- | :--- | :--- |
| **Main (시작)** | 교사 / 학급 | '게임 시작하기' 클릭 또는 '사용 설명서', '소개 영상' 클릭 | 인트로 스토리 모달 오픈 또는 설명서 슬라이드 팝업 | 시작 버튼 클릭 시 인트로 모달 오픈 |
| **Intro Story** | 교사 / 학급 | 스토리 스크롤 감상 ➔ 퀴즈 단계별 텍스트 타이핑 확인 및 정답 확인 ➔ 긴급 재난 문자 확인 | Web Audio 효과음 재생, 퀴즈 1~10단계 진행, OUTRO 전환 시 사이렌 오디오(5초) 재생 | 10단계 완료 후 시작 버튼 클릭 (또는 건너뛰기 클릭) |
| **Setup: Config** | 교사 | 라운드당 제한시간(초) 입력 (권장 420~480초) | 상태 저장 (`config.roundTime`) | '다음 단계' 버튼 클릭 |
| **Setup: Students** | 교사 | 학생 명부 텍스트 붙여넣기 및 '학생 추가하기' 클릭 | 줄바꿈 기준으로 학생 배열 생성 및 카드 렌더링 | 학생 수 >= 2명 시 '다음 단계' 활성화 |
| **Setup: Zombies** | 교사 (비밀) | 학생 카드를 클릭하여 최초 감염자(1명 이상) 선택 | `isZombie: true`, `isOriginalZombie: true` 지정 (초록 테두리) | 감염자 수 >= 1명 시 '게임 시작' 활성화 |
| **Game: In Round** | 학생 2인 / 교사 | 1) 접촉 학생 2명 카드 선택 후 [터치] 클릭<br>2) 치료제 희망자 1명 선택 후 [치료제] 클릭 | 1) 중복 여부 확인 ➔ 안전(+1점)/감염 판정 ➔ 로그 기록<br>2) 해당 라운드 감염 여부 확인 ➔ 정상 복구/유지 ➔ 로그 기록 | 타이머 만료(0초) 또는 [라운드 종료] 버튼 클릭 |
| **Game: Round End** | 교사 | 라운드 종료 오버레이에서 현황 확인 후 [다음 라운드] 클릭 | 터치 안 한 학생 강제 감염 처리 (`!touchedThisRound`), 라운드 증가 | 1R, 2R ➔ 다음 라운드 GameView<br>3R(최종) ➔ ResultsView 이동 |
| **Results** | 교사 / 학생 | 최종 결과 및 생존자 승점 랭킹 확인, 엑셀 다운로드 | 카운팅 애니메이션 후 승리 진영 선포, 개별 순위표 출력, XLSX 생성 | '처음으로 돌아가기' 클릭 시 초기화 |

---

# 3. SCREEN STRUCTURE

앱 내 구현된 전체 화면 및 모달 구성:

### 1. Main Screen (`StartView`)
* **주요 목적**: 게임의 타이틀 브랜딩 및 설명서/소개영상/시작 진입로 제공
* **주요 UI 요소**: 네온 펄스 해골 아이콘, 그라데이션 타이틀 (`VIRUS GAME`), 해시태그 뱃지 (`#지니어스 한 학급 놀이`, `#과학 감염병 단원`, `#바이러스`, `#세균`)
* **주요 버튼**:
  * `게임 시작하기` (Neon 버튼): 인트로 스토리 모달 실행
  * `사용 설명서` (Secondary 보라색 버튼, '📢 필독!' 바운스 뱃지 부착): 5장 슬라이드 팝업 실행
  * `소개 영상` (YouTube 붉은 버튼): 외부 공식 유튜브 링크(`https://youtu.be/ZqVtWvMgoQI`) 새 탭 오픈
* **연결**: 인트로 스토리 모달 (`IntroStoryModal`), 설명서 모달

### 2. Intro Story Modal (`IntroStoryModal`)
* **주요 목적**: 과학 감염병 단원 사전 퀴즈 및 긴급 재난 문자 스토리텔링을 통한 게임 몰입도 극대화
* **모드 구성 (3 Mode Lifecycle)**:
  1. `INTRO`: 화면 아래에서 위로 올라가는 스토리 텍스트 크레딧 (8초 스크롤, '다음 단계로 진행하기')
  2. `QUIZ` (1~10단계):
     * 상단 진행도 인디케이터 (1/10 ~ 10/10 STAGE)
     * 단계별 과학 이미지 (`step1_img` ~ `step10_img`)
     * 문제 타이핑 효과 (`setInterval` 45ms + Web Audio 기계식 타이핑 사운드)
     * 문제 텍스트 클릭 시 즉시 전체 보기(스킵)
     * [정답 확인하기] 클릭 시 팝업 카드 등장 (따옴표 제거된 볼드 그라데이션 정답)
     * 10단계 특수 규칙: 상단 챕터 타이틀 숨김, '정답 확인' 버튼 생략, 바로 [최종 미션 완료!] 버튼 노출
  3. `OUTRO` (긴급 재난 경보):
     * 사이렌 소리(외부 오디오 파일 재생 시도 후 Web Audio API Synth 사이렌 5초 재생 fallback)
     * 순차적 텍스트 등장 (`delay` 0.1s ~ 3.0s)
     * "접촉.. 접촉.. ➔ 📱 긴급 재난 문자 발령 ➔ 바이러스 게임을 시작합니다"
* **상단 공통 컨트롤**:
  * `건너뛰기` (FastForward 보라색 캡슐 버튼): 퀴즈를 보지 않고 즉시 게임 설정(`SETUP_CONFIG`)으로 점프
  * `음소거/소리 켜기` (Volume2 / VolumeX 버튼)
  * `닫기` (X 버튼)

### 3. Setup: Configuration (`SetupConfigView`)
* **주요 목적**: 게임 환경 변수(라운드 시간) 입력
* **주요 UI 요소**: 라운드당 제한 시간(초) 인풋 필드, 3라운드 고정 안내 카드
* **안내 문구**:
  * 💡 "라운드당 제한시간은 플레이어 수에 따라 달라지지만 7~8분을 추천드립니다."
  * 💡 "게임 중간 타이머를 멈추거나 라운드를 강제 종료할 수 있기 때문에 시간 설정은 크게 중요하지 않습니다."
* **주요 버튼**: `이전`, `다음 단계`

### 4. Setup: Students (`SetupStudentsView`)
* **주요 목적**: 참여 학생 명단 등록 및 일괄 관리
* **주요 UI 요소**: 
  * 텍스트에어리어 (한 줄에 1명씩 입력)
  * 📌 "학생 명부 한글 파일에서 학생 이름 셀만 블록 지정하신 후 합치기를 합니다. 이후 학생 이름 전체를 복사해 붙여넣기를 하시면 편리합니다." 안내 상자
  * 등록된 학생 카드 그리드 (개별 삭제 X 버튼, 전체 삭제 버튼)
* **주요 버튼**: `학생 추가하기`, `이전`, `다음 단계` (2명 이상 시 활성화)

### 5. Setup: Initial Zombies (`SetupZombiesView`)
* **주요 목적**: 교사가 비밀리에 최초 감염자를 1명 이상 지정
* **주요 UI 요소**: 학생 카드 그리드 (클릭 시 초록색 감염자 하이라이트 토글), 감염자/비감염자 수 카운터
* **주요 버튼**: `이전`, `게임 시작` (감염자 >= 1명 시 활성화)

### 6. Game Station (`GameView`)
* **주요 목적**: 라운드 진행 중 학생 접촉 및 치료제 사용 입력 처리
* **상단 헤더**:
  * `ROUND N` 뱃지
  * `라운드 종료` 버튼 (조기 마감 모달 호출)
  * `대형 타이머` (10초 미만 시 붉은색 펄스 애니메이션)
  * 타이머 컨트롤러: `일시정지/시작` 토글 버튼, `+10초` 연장 버튼
  * 블라인드 상태창: 감염자 `?`명 / 비감염자 `?`명 (학생들이 볼 수 없도록 물음표 블라인드 처리)
* **중앙 학생 그리드**:
  * 2열~6열 반응형 그리드
  * 타이머 정지 시 비활성화(Grayscale/Opacity 50%)
  * 학생 클릭 시 선택(보라색 글로우, 체크마크 뱃지, 최대 2명 선택 제한)
  * 당해 라운드 접촉 완료자 `Touched` 뱃지 표시
* **하단 플로팅 액션 바**:
  * `터치 (Zap)`: 2명 선택 시 활성화 ➔ 접촉 판정 실행
  * `치료제 (Heart)`: 1명 선택 시 활성화 ➔ 치료제 사용 확인 모달 ➔ 치료 판정 실행
* **보조 버튼**:
  * 좌측 하단 `게임 강제 종료` (비상 리셋 모달 호출)
  * 우측 하단 `교사 페이지` (비밀 관리자 모달 호출)

### 7. Teacher Management Modal (`TeacherPage`)
* **주요 목적**: 교사가 전체 학생의 진짜 정체(감염자/비감염자), 당해 라운드 접촉 여부, 승점, 전체 로그를 실시간 모니터링
* **주요 UI 요소**:
  * 학생 상태 그리드: 감염자(초록색), Touched(파란색), Infected(빨간색) 뱃지 표시
  * 실시간 타임라인 로그 창: `TOUCH`, `INFECTION`, `CURE`, `ROUND_START`, `ROUND_END`, `GAME_START` 태그별 컬러 로그
* **주요 버튼**: `결과 엑셀 다운로드`, `닫기(X)`

### 8. Round End Overlay (`GameView 내 오버레이`)
* **주요 목적**: 라운드 종료 시점 집계 및 무활동자 자동 감염 안내
* **표시 정보**: 해당 라운드 종료 후 확정된 감염자 수 vs 비감염자 수
* **주요 버튼**: `다음 라운드 시작` (1R, 2R) 또는 `결과 보기` (3R)

### 9. Results Screen (`ResultsView`)
* **주요 목적**: 3라운드 종료 후 최종 진영 승리 선포 및 생존자 개별 승점 순위 발표
* **주요 UI 요소**:
  * 숫자 롤링 애니메이션 카운터 (0부터 실제 인원수까지 점진적 카운팅)
  * 승리 진영 거대 타이포그래피 (`감염자 승리` 또는 `비감염자 승리`)
  * 비감염자 승리 시: 생존자 개별 승점 랭킹 리스트
* **주요 버튼**: `결과 엑셀 다운로드`, `처음으로 돌아가기` (새로고침 초기화)

---

# 4. TECH STACK

* **Frontend Core**:
  * **React 19 (`^19.0.0`)**: 최신 React 컴포넌트 아키텍처 및 상태 관리
  * **TypeScript (`~5.8.2`)**: 강력한 타입 안정성 보장 (`Student`, `GameLog`, `GameConfig`, `GameState`)
  * **Vite (`^6.2.0`)**: 초고속 번들링 및 개발 서버 환경
* **Styling & Design System**:
  * **Tailwind CSS v4 (`@tailwindcss/vite ^4.1.14`, `tailwindcss ^4.1.14`)**: 유틸리티 클래스 기반 다크 테마 UI 스타일링
  * **Google Fonts (`Black Han Sans`, `Do Hyeon`, `Inter`, `JetBrains Mono`)**: 가독성 높은 디스플레이 및 본문 폰트
* **Animation & Icons**:
  * **Motion (`motion ^12.38.0`)**: 모달 전환, 슬라이드 트랜지션, 뱃지 팝업, 타이핑 인디케이터
  * **Lucide React (`lucide-react ^0.546.0`)**: 일관된 UI 아이콘 시스템
* **Data Processing & Export**:
  * **SheetJS (`xlsx ^0.18.5`)**: 클라이언트 사이드 즉시 실행 가능한 정밀 엑셀 스프레드시트(`.xlsx`) 생성 엔진
* **Audio Processing**:
  * **Web Audio API (`window.AudioContext`)**: 외부 음원 파일 로딩 실패 시에도 100% 동작하는 순수 주파수 합성(Oscillator/Gain) 사운드 엔진 (타이핑음, 정답음, 경고음, 다음음, 5초 사이렌 스윕음)
  * **HTML5 Audio (`new Audio()`)**: 외부 업로드 사이렌 음원 파일 우선 탐색 재생

---

# 5. SYSTEM ARCHITECTURE

본 서비스는 **단일 기기 교사 주도형 키오스크(Single-Device Host/Station Architecture)** 아키텍처를 채택하였다.

```
┌──────────────────────────────────────────────────────────────┐
│                    Single Device (Kiosk / PC)                │
│                                                              │
│  ┌────────────────────────────────────────────────────────┐  │
│  │                authoritative State (Memory)            │  │
│  │  - students: Student[]                                 │  │
│  │  - touchHistory: string[]                              │  │
│  │  - logs: GameLog[]                                     │  │
│  │  - currentRound, timeLeft, isTimerRunning              │  │
│  └────────────────────────────────────────────────────────┘  │
│                               ▲                              │
│                               │ Action Dispatch              │
│                               ▼                              │
│  ┌────────────────────────────────────────────────────────┐  │
│  │                   UI Presentation Layer                │  │
│  │  ├─ Public Display (학생 공개용 메인 화면)              │  │
│  │  │   - 블라인드 현황 (감염자: ?, 비감염자: ?)            │  │
│  │  │   - 타이머, 학생 그리드, 터치/치료제 액션 버튼      │  │
│  │  │                                                     │  │
│  │  ├─ Teacher Modal (교사용 비밀 관리 화면, z-[100])     │  │
│  │  │   - 학생별 실제 감염 여부 및 승점                   │  │
│  │  │   - 전체 타임라인 로그, 엑셀 내보내기               │  │
│  │  │                                                     │  │
│  │  └─ Story / Guide / Results Overlays                   │  │
│  └────────────────────────────────────────────────────────┘  │
└──────────────────────────────────────────────────────────────┘
```

* **Authoritative State의 위치**: 브라우저 메인 메모리 (`React.useState` in `App.tsx`)
* **Host의 역할**: 교사 PC 또는 교탁에 거치된 태블릿 1대가 전체 게임의 호스트이자 입력 키오스크 역할을 동시에 수행
* **클라이언트 분리 여부**: 학생 개별 폰 접속이 없는 **오프라인 대면 터치 방식**이므로 복잡한 네트워크 통신 오버헤드와 접속 끊김 위험을 완전히 배제함
* **화면 간 역할 분리**: 학생들에게 보이는 프로젝터 뷰에서는 감염 정보가 철저히 은폐되며, 교사 관리 모달에서만 모든 상태가 투명하게 공개됨

---

# 6. NETWORK ARCHITECTURE

`NOT USED`

* 본 서비스는 인터넷 환경이 불안정하거나 학생 스마트폰 휴대가 금지된 학교 교실 환경의 특성을 고려하여 **완전 독립형(Stand-alone / Offline-first) 단일 키오스크 모델**로 설계되었다.
* WebRTC, PeerJS, WebSocket, Firebase 등의 네트워크 통신을 사용하지 않으므로 네트워크 지연, 룸 코드 유실, 방 재접속 오류가 원천적으로 발생하지 않는다.

---

# 7. STATE & DATA MODEL

### 핵심 인터페이스 (`/src/types.ts`):

```typescript
export interface Student {
  id: string;                  // 고유 ID (Math.random string)
  name: string;                // 학생 이름
  isZombie: boolean;           // 현재 감염자 여부
  isOriginalZombie: boolean;   // 최초 지목된 감염자 여부 (치료제 무효화 판정용)
  infectedThisRound: boolean;  // 이번 라운드에 감염되었는지 여부 (치료제 적용 가능 판정용)
  points: number;              // 누적 승점 (인간-인간 접촉 시 +1)
  touchedThisRound: boolean;   // 이번 라운드 접촉 완료 여부 (무활동 패널티 판정용)
}

export interface GameLog {
  id: string;
  timestamp: string;           // 발생 시각
  round: number;               // 라운드 번호
  message: string;             // 요약 메시지
  type: 'TOUCH' | 'CURE' | 'INFECTION' | 'ROUND_START' | 'ROUND_END' | 'GAME_START';
  // 엑셀 내보내기용 상세 필드
  student1?: string;
  status1?: string;            // '감염자' | '비감염자'
  student2?: string;
  status2?: string;
  pointsAwarded?: number;      // 지급된 승점
  cumulativePoints?: number;   // 누적 승점
  isOriginalZombie?: boolean;  // 최초 감염자 여부
  vaccineUsed?: boolean;       // 백신 사용 여부
}

export type GameState = 'START' | 'SETUP_CONFIG' | 'SETUP_STUDENTS' | 'SETUP_ZOMBIES' | 'GAME' | 'RESULTS';

export interface GameConfig {
  roundTime: number;           // 라운드 제한시간 (초 단위)
  totalRounds: number;         // 총 라운드 수 (3으로 고정)
}
```

### 데이터 수명 주기 (Data Lifecycle):

| 데이터 항목 | 저장 위치 | 수명 주기 및 삭제 시점 |
| :--- | :--- | :--- |
| `students` (학생 목록 & 상태) | Memory (`App.tsx`) | 브라우저 탭 유지 동안 보존, '처음으로 돌아가기' 또는 강제 종료 시 리셋 |
| `touchHistory` (중복 터치 쌍) | Memory (`App.tsx`) | 게임 진행 동안 누적 (`id1-id2`), 게임 리셋 시 삭제 |
| `logs` (게임 로그) | Memory (`App.tsx`) | 게임 내내 누적, 교사 페이지 및 엑셀 다운로드로 영구 파일 저장 가능 |
| `config` (시간 설정) | Memory (`App.tsx`) | 설정 단계에서 지정, 게임 중 참조 |
| `IntroStoryModal State` | Memory (Local State) | 모달 열릴 때마다 1단계부터 초기화 |

---

# 8. GAME / SERVICE LOGIC

### 1. 접촉(Touch) 판정 로직
* 2명의 학생 ID(`id1`, `id2`)가 선택되었을 때 실행:
  1. **중복 검사**: `pairId = [id1, id2].sort().join('-')`를 생성하고 `touchHistory`에 존재하는지 확인 ➔ 존재 시 경고 모달 출력 후 중단
  2. **감염자 포함 여부 검사**:
     * `s1.isZombie || s2.isZombie`인 경우:
       * 비감염자는 `isZombie = true`, `infectedThisRound = true`로 변경 (승점 획득 0)
       * 이미 감염자였던 학생은 상태 유지
       * 두 학생 모두 `touchedThisRound = true` 설정
       * 로그 기록: `[s1.name]와(과) [s2.name] 터치! 감염 발생 (승점 없음)`
     * 두 학생 모두 비감염자인 경우:
       * 두 학생 모두 `points = points + 1`, `touchedThisRound = true` 설정
       * 로그 기록: `[s1.name]와(과) [s2.name] 터치! 안전 (+1점)`
  3. `touchHistory`에 `pairId` 추가 및 선택 해제

### 2. 치료제(Cure) 사용 로직
* 1명의 학생 ID가 선택되었을 때 확인 모달 후 실행:
  * 학생이 당해 라운드 감염자(`infectedThisRound === true`)인 경우:
    * `isZombie = false`, `infectedThisRound = false`로 즉시 복구 (이전 획득 승점 유지)
    * 로그 기록: `[student.name] 치료제 사용! 비감염자로 복구`
  * 학생이 최초 감염자(`isOriginalZombie === true`)이거나 이전 라운드부터 이미 감염자였던 경우:
    * 상태 변화 없음
    * 로그 기록: `[student.name] 치료제 사용! 변화 없음`

### 3. 라운드 종료 및 무활동 패널티 로직
* 타이머 종료 또는 라운드 강제 종료 시 실행:
  * 모든 학생을 순회하며 `!s.touchedThisRound`인 학생을 검사
  * 이번 라운드에 터치를 하지 않은 학생은 자동으로 `isZombie = true`로 전환
  * 다음 라운드를 위해 `infectedThisRound = false`, `touchedThisRound = false`로 초기화
  * 라운드 증가(`currentRound + 1`) 및 타이머 재설정

### 4. 승패 판정 로직
* 3라운드 완료 후 `ResultsView`에서 판정:
  * `actualHumans = students.filter(s => !s.isZombie).length`
  * `actualHumans === 0` ➔ **감염자 승리 (`ZOMBIE`)**
  * `actualHumans > 0` ➔ **비감염자 승리 (`HUMAN`)**
  * 비감염자 승리 시, 생존한 비감염자들을 `points` 내림차순 정렬하여 개별 순위표 출력

---

# 9. UI / UX DECISIONS

1. **교실 프로젝터 가시성 극대화 (High Contrast & Large Typography)**:
   * 칠판이나 빔프로젝터로 멀리서 보는 교실 환경을 위해 어두운 배경(Deep Black/Zinc-900)에 고대비 네온 컬러(Green, Purple, Red, Yellow)와 굵은 폰트(`Black Han Sans`, `Do Hyeon`) 적용
2. **학생 정체 블라인드 처리 (Anti-Cheating UX)**:
   * 프로젝터에 상시 노출되는 메인 게임 화면에서는 감염자 수와 비감염자 수를 물음표(`?`)로 표시하여 학생들의 심리 추리와 비밀 활동 보호
3. **교사용 비밀 관리 모달 (Independent Teacher Overlay)**:
   * 교사가 학생들 모르게 실제 진행 상황을 파악하거나 엑셀을 내려받을 수 있도록 독립된 오버레이 모달(`z-[100]`)로 분리
4. **오작동 방지 액션 버튼 인터랙션 (State-Driven Floating Action Bar)**:
   * 2명이 정확히 선택되었을 때만 `터치` 버튼이 활성화(Scale-100, Opacity-100)되고, 1명일 때는 `치료제` 버튼만 활성화
   * 타이머가 일시정지 상태일 때는 실수로 학생 카드를 누르지 못하도록 전체 그리드를 Grayscale 50% 처리
5. **유연한 교실 수업 운영을 위한 타이머 컨트롤**:
   * 교실 상황에 따라 활동 설명이 길어지거나 일찍 끝날 수 있으므로 `일시정지`, `+10초`, `라운드 강제 종료` 버튼을 원클릭으로 배치
6. **인트로 모달 건너뛰기 (`FastForward` Button)**:
   * 이미 규칙을 알고 있는 2회차 플레이나 빠른 수업 진행을 위해 인트로 스토리 및 퀴즈를 언제든 원클릭으로 스킵할 수 있는 상단 고정 버튼 구현
7. **한글(HWP) 명부 복사 편의성 안내**:
   * 교사들이 NEIS나 한글 표에서 학생 이름을 복사할 때 겪는 서식 문제를 방지하기 위해 '셀 합치기 후 복사' 팁을 눈에 띄는 앰버색 안내 상자로 명시

---

# 10. ASSET & AUDIO STRUCTURE

### 1. 이미지 에셋 구조 (`/src/assets/images/`):
* `back_bg_1784754202433.jpg`: 인트로/아웃트로 공통 백그라운드 이미지
* `step1_img_1784754228525.jpg` ~ `step10_img_1784754362358.jpg`: 1~10단계 퀴즈별 과학 주제 일러스트레이션 (감염병 정의, 종류, 격리, 생명 위협, 비말, 공기, 음식, 접촉, 예방 수칙)

### 2. 오디오 에셋 및 합성 아키텍처:
* **이중 오디오 폴백 시스템 (Dual-Layer Audio Engine)**:
  1. **HTML5 Audio Layer**:
     * 긴급 재난 문자(`OUTRO`) 단계 진입 시 `/siren.mp3`, `/siren.wav`, `/emergency.mp3`, `/alert.mp3` 등 외부 음원 파일을 우선 탐색하여 5초간 재생 후 자동 정지
  2. **Web Audio API Pure Synthesis Layer (내장 무중단 합성기)**:
     * 외부 파일이 없거나 브라우저 보안으로 차단될 경우 Web Audio API의 `OscillatorNode`와 `GainNode`를 사용하여 100% 브라우저 자체 합성음 재생:
       * `type`: 타자기 타이핑음 (440~640Hz 무작위 주파수 Triangle 파형)
       * `confirm`: 정답 확인음 (523Hz ➔ 1046Hz Sine 파형 옥타브 상승)
       * `alert`: 경고음 (880Hz ➔ 440Hz Sawtooth 파형)
       * `next`: 다음 단계음 (659Hz Sine 파형)
       * `siren`: 5초 동안 550Hz ➔ 980Hz ➔ 550Hz 주파수를 0.8초 주기로 왕복 스윕하는 듀얼 Sawtooth/Square 사이렌 파형
* **브라우저 자동재생 정책 대응**:
  * `AudioContext.state === 'suspended'`일 경우 사용자 인터랙션 발생 시 자동으로 `ctx.resume()` 호출
  * 전체 음향을 즉시 차단할 수 있는 음소거(`VolumeX`) 버튼 상시 제공

---

# 11. PROBLEMS ENCOUNTERED

## Problem 1: 교실 환경에서 외부 효과음 파일 누락 시 사이렌 소리 미출력

* **문제 현상**: 인트로 스토리 마지막의 긴급 재난 문자 단계에서 사이렌 효과음이 재생되지 않음.
* **원인**: 웹 애플리케이션 환경에 외부 음원 파일(mp3/wav)이 번들링되어 있지 않거나 경로가 맞지 않아 HTML5 Audio 로드가 실패함.
* **처음 시도한 해결 방법**: 1회성 1초짜리 단일 주파수 오실레이터 사운드를 호출함.
* **왜 충분하지 않았는지**: 1초 만에 소리가 끊기고 긴장감 있는 실제 재난 경보 사이렌 느낌이 나지 않았음.
* **최종 해결 방법**: 
  1. 외부 음원 파일 후보 경로(`/siren.mp3`, `/emergency.mp3` 등)를 순차 탐색하여 재생하는 `playOutroAudioFile` 함수 구현.
  2. 외부 파일이 없을 경우 `Web Audio API`를 통해 5초 동안 주기적으로 550Hz~980Hz를 왕복 변조하는 듀얼 오실레이터 합성 사이렌(`playSynthSiren`)으로 자동 폴백되도록 구현.
* **교훈**: 교실용 웹앱은 외부 오디오 파일 의존성을 최소화하고, Web Audio API를 이용한 자체 사운드 합성기(Synthesizer)를 반드시 백업으로 탑재해야 한다.

---

## Problem 2: 인트로 퀴즈 진행이 불필요한 상황에서의 시간 지체

* **문제 현상**: 이미 이전 시간에 퀴즈를 푼 학급이나 테스트 시 인트로 모달 10단계를 모두 거쳐야만 게임 설정으로 넘어갈 수 있어 수업 시간이 낭비됨.
* **원인**: 인트로 모달의 단계 완료 이벤트(`isStage10 완료`)에서만 게임 설정으로 뷰가 전환되도록 강제되어 있었음.
* **처음 시도한 해결 방법**: 텍스트 타이핑 스킵 기능만 추가함.
* **왜 충분하지 않았는지**: 여전히 10번의 '다음' 버튼을 연속으로 클릭해야 했음.
* **최종 해결 방법**: 인트로 모달 우측 상단에 `건너뛰기 (FastForward)` 버튼을 신설하여 클릭 즉시 `onStartGame()`을 호출, 바로 `SETUP_CONFIG` 화면으로 직행하도록 수정함.
* **교훈**: 교육용 앱의 스토리/튜토리얼 레이어에는 교사의 수업 템포 조절을 위한 즉시 스킵(Fast-pass) 수단을 반드시 마련해야 한다.

---

## Problem 3: 학생 명부 복사-붙여넣기 시 서식 깨짐 문제

* **문제 현상**: 교사들이 한글(HWP) 표나 엑셀에서 명부를 긁어와 붙여넣을 때 탭 문자나 공백으로 인해 학생 이름이 비정상적으로 파싱됨.
* **원인**: 표 형태의 데이터를 복사하면 줄바꿈(`\n`)이 아닌 탭(`\t`)으로 구분되거나 불필요한 공백이 포함됨.
* **처음 시도한 해결 방법**: 개별 텍스트 인풋으로 한 명씩 추가하도록 유도.
* **왜 충분하지 않았는지**: 25~30명의 학생 이름을 하나씩 치는 것은 교사에게 극심한 피로를 유발함.
* **최종 해결 방법**: 
  1. 벌크 텍스트에어리어에서 `split('\n').map(n => n.trim()).filter(Boolean)`으로 정제.
  2. 한글 파일에서 학생 이름 셀만 블록 지정 후 합치기를 한 뒤 복사하라는 실무 팁 안내 박스(`📌 안내`)를 UI에 명시.
* **교훈**: 교사의 실무 업무 툴(한글, 나이스, 엑셀)과의 데이터 호환성을 고려한 입력 UX 가이드를 제공해야 한다.

---

# 12. IMPORTANT CHANGE HISTORY

## Change 1: 메인 화면 헤더 브랜딩 및 태그라인 수정
* **Initial**: 일반적인 게임 타이틀 및 단순 서브타이틀
* **Changed To**: `#지니어스 한 학급 놀이`, `#과학 감염병 단원`, `#바이러스`, `#세균` 해시태그 뱃지 및 유튜브 공식 소개 영상 링크 버튼 추가
* **Reason**: 과학과 교과 수업 및 창의적 체험활동(학급 놀이) 연계 목적을 직관적으로 어필하고, 교사/학생이 영상 룰을 바로 확인할 수 있도록 지원하기 위함.
* **Final Status**: 확정 적용됨.

## Change 2: 인트로 스토리 모달 10단계 특수 처리
* **Initial**: 1~10단계 모두 동일하게 상단 챕터명 표시 및 '정답 확인' 버튼 노출
* **Changed To**: 10단계에서는 상단 챕터명을 숨기고, '정답 확인' 버튼을 생략한 채 바로 `[최종 미션 완료!]` 버튼 노출
* **Reason**: 10단계는 질문-정답 형태가 아닌 "수칙을 지켜서 건강한 생활을 해요!"라는 최종 슬로건이므로 불필요한 퀴즈 확인 인터랙션을 제거함.
* **Final Status**: 확정 적용됨.

## Change 3: 인트로 모달 폰트 및 연출 정돈
* **Initial**: 복잡한 그라데이션 텍스트와 과도한 줌 애니메이션
* **Changed To**: 차분하고 긴장감 있는 `Do Hyeon` 폰트 적용, 배경 이미지 줌 제거 및 부드러운 페이드 전환, 재난 문자 박스 순차 딜레이 등장
* **Reason**: 가독성을 높이고 시각적 피로도를 낮추면서도 재난 상황의 긴장감을 극대화함.
* **Final Status**: 확정 적용됨.

---

# 13. REUSABLE PATTERNS

### 1. Single-Device Kiosk Contact-Judge Pattern (단일 기기 대면 판정 패턴)
* **목적**: 학생 기기 없이 교탁 앞 1대 태블릿/PC로 1대1 상호작용 및 비밀 룰을 완벽히 통제
* **적용 조건**: 학급 단위 소셜 디덕션 게임, 비밀 정체가 존재하는 역할극 활동
* **핵심 구조**: 
  * 화면 중앙: 학생 카드 그리드 (동시 다중 선택 제어)
  * 화면 하단: 선택된 인원수(1명 vs 2명)에 따라 조건부 활성화되는 인터랙티브 플로팅 액션 바
  * 상태 은폐: 학생 공개 뷰는 물음표(`?`), 교사용 모달은 완전 공개

### 2. Dual-Layer Resilient Audio Synthesizer Pattern (이중 계층 오디오 합성 패턴)
* **목적**: 외부 미디어 파일 로드 실패나 오프라인 환경에서도 100% 사운드 피드백 보장
* **적용 조건**: BGM, 사이렌, 퀴즈 효과음이 필수적인 웹 기반 교육 게임
* **핵심 구조**:
  * 1단계: HTML5 `new Audio('/path.mp3')` 시도
  * 2단계: 실패(catch) 또는 파일 부재 시 즉시 `Web Audio API` 오실레이터(Sine/Sawtooth/Square) 주파수 스윕 알고리즘 가동

### 3. Detailed Forensic Excel Log Pattern (정밀 감사 엑셀 로그 패턴)
* **목적**: 게임 진행 중 발생한 모든 턴의 상세 데이터를 사후 분석 및 수행평가 자료로 내보내기
* **적용 조건**: 규칙 분쟁 방지, 학급 활동 평가, 데이터 기반 게임
* **핵심 구조**:
  * 단순 메시지 문자열 외에 `student1`, `status1`, `student2`, `status2`, `pointsAwarded`, `cumulativePoints`, `isOriginalZombie`, `vaccineUsed`를 구조화하여 기록
  * `xlsx` 라이브러리로 클릭 즉시 2차원 배열 시트 변환 및 다운로드

---

# 14. SERVICE-SPECIFIC ELEMENTS

* **지니어스 바이러스 게임 고유 판정 룰**:
  * 인간 + 인간 ➔ 안전 및 각 1점 획득
  * 인간 + 좀비 ➔ 인간 감염 및 0점
  * 좀비 + 좀비 ➔ 상태 유지 및 0점
* **최초 감염자(Original Zombie) 영구 불치 룰**:
  * 게임 시작 전 지정된 최초 감염자는 백신(치료제)을 사용해도 비감염자로 돌아갈 수 없음
* **무활동자 강제 감염 룰**:
  * 제한 시간 동안 아무하고도 접촉하지 않은 플레이어는 라운드 종료 시 자동 감염

---

# 15. DO NOT REPEAT

1. **브라우저 기본 `window.alert`, `window.confirm` 사용 금지**:
   * 교실 전자칠판 터치 환경에서 브라우저 팝업은 작아서 보이지 않고 UX를 심각하게 저해함. 반드시 앱 내 커스텀 `AnimatePresence` 모달을 사용할 것.
2. **외부 오디오 URL/파일에만 의존하는 사운드 구현 금지**:
   * 학교 전산망 방화벽이나 로컬 환경에서 외부 음원이 차단되면 게임 분위기가 급격히 가라앉음. 반드시 Web Audio API 기반 오실레이터 사운드를 내장할 것.
3. **학생 화면에 감염자 실시간 숫자 노출 금지**:
   * 학생들에게 감염자 숫자가 실시간으로 보이면 누가 감염되었는지 역추적이 쉬워져 심리전의 재미가 반감됨. 라운드 중에는 물음표(`?`)로 블라인드 유지할 것.

---

# 16. CLASSROOM / REAL-WORLD LESSONS

* **교실 이동 동선 관리**: 학생 2명이 교탁 앞으로 나와 교사 앞에서 터치를 누르고 들어가는 턴 방식을 취하므로, 줄을 서거나 접촉 후 돌아가는 동선 지도가 필요함.
* **시간 설정의 유연성**: 학급마다 토론과 접촉에 걸리는 시간이 천차만별이므로 7~8분을 기본으로 하되, 활동이 마무리되면 교사가 즉시 '라운드 종료'를 눌러 시간을 아끼는 것이 효과적임.
* **치료제 남용 방지**: 1인당 치료제는 1회로 제한되어 있으므로, 교사가 치료제 사용 시 신중하게 확인 모달을 거치도록 한 설계가 유효했음.

---

# 17. GGDS CANDIDATES

### 1. Zero-Network Single-Kiosk Mode
* **등급**: `MUST`
* **이유**: 초·중등학교 교실은 학생 개인 디바이스 준비, 와이파이 접속 불량, 화면 미러링 지연 등 네트워크 이슈가 가장 빈번하므로, 단일 기기 오프라인 키오스크 모드는 가장 안전한 기본 표준이어야 함.

### 2. Built-in Web Audio Oscillator Fallback
* **등급**: `MUST`
* **이유**: 효과음은 학생 몰입의 핵심 요소이나 외부 음원 링크는 쉽게 깨지므로 Web Audio API를 활용한 무중단 사운드 합성을 필수 탑재해야 함.

### 3. Fast-Forward / Skip Control on Story/Intro
* **등급**: `SHOULD`
* **이유**: 수업 재진행, 이전 차시 복습, 리셋 상황에서 인트로 튜토리얼 강제 진행은 교사의 수업 진행을 방해하므로 상시 건너뛰기 기능이 제공되어야 함.

### 4. High-Contrast Dark Display with Color Blind Consideration
* **등급**: `SHOULD`
* **이유**: 교실 빔프로젝터는 주변 조명에 의해 물 빠진 색감으로 보이기 쉬우므로 고대비 다크 배경과 굵은 원색(Green, Red, Purple) 및 텍스트 뱃지 병기를 권장함.

### 5. Detailed XLSX Forensic Log Export
* **등급**: `OPTIONAL`
* **이유**: 단순 레크리에이션 게임에서는 불필요할 수 있으나, 학급 평가 및 룰 분쟁 방지가 필요한 전략/심리 게임에서는 매우 유용함.

---

# 18. FINAL IMPLEMENTATION STATUS

* **정상 구현됨 (Fully Implemented)**:
  * 인트로 스토리 모달 (10단계 퀴즈, 타이핑 효과, 스킵, 아웃트로 사이렌 및 5초 음원 재생 fallback)
  * 사용 설명서 5장 대형 팝업 슬라이드
  * 라운드 시간 설정 및 3라운드 고정 셋업
  * 학생 명단 벌크 입력 및 개별/전체 삭제
  * 최초 감염자 비밀 지목 토글
  * 2인 접촉 판정 엔진 및 중복 터치 차단
  * 1인 치료제 사용 판정 및 확인 모달
  * 타이머 컨트롤 (일시정지/재개, +10초, 조기 라운드 종료)
  * 블라인드 메인 화면 & 교사용 비밀 종합 대시보드
  * 무활동 플레이어 자동 감염 로직
  * 결과 발표 롤링 카운터 애니메이션 및 승자 선포
  * 생존자 승점 랭킹 리스트
  * 전체 히스토리 엑셀(.xlsx) 파일 내보내기
  * 상단 인트로 건너뛰기 버튼 (`FastForward`)
* **미구현 / 제거됨**:
  * P2P / 학생 개별 모바일 접속 (단일 키오스크 정책에 따라 의도적으로 배제)
* **확인 불가 (UNCONFIRMED)**: 없음 (모든 코드가 단일 리포지토리 내에서 완전 검증됨)

---

# 19. FINAL REFERENCE SUMMARY

* **가장 중요한 기술 구조**: React 19 + TypeScript 기반의 단일 기기(Single-Station) 메모리 중심 상태 머신과 Web Audio API 내장 합성 사운드 엔진.
* **가장 중요한 UX 원칙**: 프로젝터 공개 화면과 교사용 비밀 화면의 철저한 정보 분리, 그리고 현장 수업 속도에 맞춘 타이머 및 인트로 스킵 제어권 제공.
* **가장 가치 있는 재사용 패턴**: 2인 접촉 & 1인 아이템 판정 플로팅 액션 바, 중복 접촉 방지 쌍 해싱(`[id1, id2].sort().join('-')`), Web Audio 듀얼 오실레이터 사이렌 합성기.
* **가장 중요한 실패 사례**: 외부 음원 파일 미탑재로 인한 사이렌 불발 이슈 ➔ Web Audio API 5초 주기 스윕 합성 폴백으로 완벽 해결.
* **이 서비스를 레퍼런스로 사용하기 좋은 상황**: 마피아/지니어스 류의 사회적 추리 게임, 비밀 역할이 존재하는 학급 놀이, 단일 전자칠판으로 진행하는 대면 과학/사회 교과 활동.
* **다른 서비스에 복사하면 안 되는 고유 요소**: 최초 감염자 영구 불치 규칙, 3라운드 고정 제한, 감염병 교과 전용 10단계 퀴즈 데이터.
